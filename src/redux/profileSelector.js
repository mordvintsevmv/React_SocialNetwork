export const getPostData = (state) => {
    return state.profilePage.postData
}

export const getCurrentPost = (state) => {
    return state.profilePage.currentPost
}

export const getProfile = (state) => {
    return state.profilePage.profile
}